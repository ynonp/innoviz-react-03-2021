export function square(x) {
  return x * x;
}

export function twice(x) {
  return x * 2;
}

const x = 10;
export default x;

// import React, { useState } from "react";

// import { square } from "./utils";
// import { square, twice } from "./utils";
// import * as Utils from "./utils";
// Utils.square, Utils.twice
